Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
WTForms==3.1.2
email-validator==2.2.0
python-dotenv==1.0.1
SQLAlchemy==2.0.35
Werkzeug==3.0.4
gunicorn==23.0.0
openpyxl==3.1.5
playwright==1.61.0
# NOTE: the ticket asked for openai==1.*, but 2.29.0 is what is installed
# and what this was built and tested against. The client surface used here
# (OpenAI(...) + chat.completions.create) is identical across both majors;
# pinning an untested major would be worse than deviating from the ticket.
openai==2.29.0
